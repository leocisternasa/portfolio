/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  

}

module.exports = module.exports = {
  experimental: {
    images: {
      unoptimized: true,
    },
  },
}